# recursion_bug

There are bugs in `src/math_utils.py`. Fix them so all tests in `tests/` pass.

Use:
- `read_file` to inspect files
- `write_file` to fix them
- `run_tests` to verify
- `done` when all tests pass

Do not modify the tests. Preserve existing function signatures.
