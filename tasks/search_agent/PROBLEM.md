Investigate why authenticated API requests intermittently time out after a
token refresh. Use search over the local corpus, select only the useful
snippets, and produce a short root-cause summary with the mitigation.
