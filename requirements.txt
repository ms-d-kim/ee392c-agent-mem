# Inference engine
# Pinned to CUDA-12-compatible builds (driver 570.x supports CUDA 12.8).
# vllm 0.10.2 pulls torch 2.8.0+cu128. Newer vllm (>=0.20) requires CUDA 13.
# transformers must stay <5: vllm 0.10.2 references Tokenizer APIs removed in 5.x.
vllm==0.10.2
torch==2.8.0
transformers>=4.55.2,<5
tokenizers>=0.21.1,<0.22

# Agent framework
langgraph>=0.2.0
langchain-core>=0.3.0
openai>=1.50.0

# Telemetry
nvidia-ml-py>=12.0.0
psutil>=6.0.0
nvtx>=0.2.10

# Data + analysis
pandas>=2.2.0
pyarrow>=17.0.0
numpy>=1.26.0
matplotlib>=3.9.0
seaborn>=0.13.0

# Notebook
jupyter>=1.1.0
ipykernel>=6.29.0

# Utility
huggingface-hub>=0.25.0
tqdm>=4.66.0
